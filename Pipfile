[[source]]
name = "pypi"
url = "https://pypi.org/simple"
verify_ssl = true

[dev-packages]
pytest = "*"
pytest-cov = "*"
timeout-decorator = "*"
flake8 = "*"
mypy = "*"

[packages]
dlib = "*"
opencv-python = "*"

[requires]
python_version = "3.7"

[scripts]
lint = "pipenv run flake8; pipenv run mypy faceDetection/*.py tests/*.py main.py"
test = "pipenv run pytest"
